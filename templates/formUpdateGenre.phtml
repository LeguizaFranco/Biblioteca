<?php
require_once './templates/layouts/header.phtml';
?>

<div class="container mt-4">
    <h1 class="text-center mb-4">Editar Género</h1>
    <?php if (!empty($errors)): ?>
        <div class="alert alert-danger">
            <ul>
                <?php foreach ($errors as $error): ?>
                    <li><?= htmlspecialchars($error, ENT_QUOTES, 'UTF-8'); ?></li>
                <?php endforeach; ?>
            </ul>
        </div>
    <?php endif; ?>

    <form action="updateGenre/<?= $genre->id_genero ?>" method="POST">
        <div class="mb-3">
            <label for="genero" class="form-label">Titulo del Género</label>
            <input type="text" name="genero" id="genero" class="form-control" value="<?= htmlspecialchars($genre->genero, ENT_QUOTES, 'UTF-8'); ?>" required>
        </div>

        <div class="mb-3">
            <label for="descripcion" class="form-label">Descripción</label>
            <textarea name="descripcion" id="descripcion" class="form-control" rows="4" required><?= htmlspecialchars($genre->descripcion, ENT_QUOTES, 'UTF-8'); ?></textarea>
        </div>

        <div class="mb-3">
            <label for="generos_relacionados" class="form-label">Géneros Relacionados</label>
            <input type="text" name="generos_relacionados" id="generos_relacionados" class="form-control" value="<?= htmlspecialchars($genre->generos_relacionados, ENT_QUOTES, 'UTF-8'); ?>" required>
        </div>

        <div class="mb-3">
            <label for="nivel_popularidad" class="form-label">Nivel de Popularidad</label>
            <input type="number" name="nivel_popularidad" id="nivel_popularidad" class="form-control" value="<?= $genre->nivel_popularidad ?>" required>
        </div>

        <div class="text-end">
            <button type="submit" class="btn btn-primary">Guardar Cambios</button>
            <a href="genres" class="btn btn-secondary">Cancelar</a>
        </div>
    </form>
</div>